import { CalcularPuntosPipe } from './calcular-puntos.pipe';

describe('CalcularPuntosPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularPuntosPipe();
    expect(pipe).toBeTruthy();
  });
});
